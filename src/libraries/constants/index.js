const CollectionNames = require('./collectionNames.constant');
const ModelNames = require('./modelNames.constant');
const UserRoles = require('./userRoles.constant');

module.exports = {
  CollectionNames,
  ModelNames,
  UserRoles,
}